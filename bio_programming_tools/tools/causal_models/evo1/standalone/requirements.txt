# Evo1 dependencies
# https://github.com/evo-design/evo
# Note: torch is installed in setup.sh before this file.
# flash-attn is pinned here to ensure pre-built wheels exist for the torch version
# pinned in setup.sh. Update both together when bumping versions.

evo-model
flash-attn==2.8.0.post2
transformers>=4.48,<5
tqdm
