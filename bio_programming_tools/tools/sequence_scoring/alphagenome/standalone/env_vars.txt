# Environment variables for AlphaGenome (genomic sequence scoring)
#
# Note: Compute dependency variables (RECOMMENDED_JAX_SPEC, RECOMMENDED_JAX_VARIANT,
# DETECTED_CUDA_VERSION, etc.) are automatically injected by the centralized detection
# system and don't need to be listed here. Users can still override by setting
# ALPHAGENOME_JAX_SPEC, ALPHAGENOME_USE_LOCAL_CUDA_ENV, etc. in their environment.
#
# AlphaGenome checkpoint paths are now managed by BPT_MODEL_CACHE
# (passed through automatically by persistent_worker.py)

[set]
LD_LIBRARY_PATH=${VENV_PATH}/cuda_env/lib
